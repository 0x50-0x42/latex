# URL in bibliography flows out of the margin

* Solution: [https://tex.stackexchange.com/a/256190](https://tex.stackexchange.com/a/256190).

* `\def\UrlBreaks{\do\/\do-}` allows breaking at slashes at `/` and `-` in the URL.

---
