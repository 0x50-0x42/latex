# Page containing the chapter doesn't show the fancy styles in `book` class

* Solution: Use package `titlesec` and then the command `\assignpagestyle{\chapter}{fancy}`.

* There also other solutions [here](https://latex.org/forum/viewtopic.php?t=8989).
